<form method="post" action="{{ path('app_cheese_delete', {'id': cheese_listing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cheese_listing.id) }}">
    <button class="btn">Delete</button>
</form>
